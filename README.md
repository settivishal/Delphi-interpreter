# Tests
## Commands to execute test cases
Clean the project
- ```mvn clean```

Compile the project
- ```mvn compile```

Run the program
- ```mvn exec:java -D"exec.mainClass"="org.example.Interpreter" -D"exec.args"="test1 test2"```